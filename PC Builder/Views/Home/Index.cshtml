@model IEnumerable<PC_Builder.Models.CPU_Manufacturer>

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>PC Builder</title>
    </head>
    <body>
        <p>Процессор</p>
        <form>
            <select>
                @foreach (var item in Model)
                {
                    <option>@item.Manufacturer</option>
                }
            </select>
        </form>
    </body>
</html>